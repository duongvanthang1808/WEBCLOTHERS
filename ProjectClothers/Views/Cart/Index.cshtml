@model List<ProjectClothers.Models.Function.Cart>
@*kk*@
<div class="add-to-cart">
    <!-- container -->
    <div class="container">
        @if (Model.Count == 0)
        {
            <h3>Your Shopping Cart is empty.</h3>
            <p>
                Lorem ipsum dolor sit amet enim. Etiam ullamcorp uspendisse a pellentesque.
                Etiam ullamcorp uspendisse a pellentesque.Lorem ipsum dolor sit amet enim.
                Your Bascket Is Empty Please Go <a href="/men-1-accessories">here</a> And Shop
            </p>
        }
        else
        {
            <h3>Shopping Basket</h3>
            <div class="table-responsive">
                <table class="table table-striped table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Quantity</th>
                            <th>Price</th>
                            <th>Delete</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
            decimal? a = 0;
            foreach (var item in Model)
            {
                <tr>
                    <td>@item.product.ID</td>
                    <td>@item.product.Name</td>
                    <td>@item.quantity</td>
                    <td>@((item.product.Price.GetValueOrDefault(0) * item.quantity).ToString())$</td>
                    <td><button data-id="@item.product.ID" class="btn btn-primary deletecart">X</button></td>
                </tr>
                a += ((item.product.Price.GetValueOrDefault(0) * item.quantity));
            }
            <tr><td>Total:@a.ToString();</td></tr>
                        }
                    </tbody>
                </table>
            </div>
    <a type="button" class="btn btn-primary" href="/men-4-jeans">Go back shop!</a>
            ///fafdsfasfdssdsdfaddsdadss
        }
    </div>
  
</div>
<script src="~/Content/client/cart/shopcart.js"></script>